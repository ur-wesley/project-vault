export type MiseToolDto = {
  name: string;
  version: string;
  source: string;
  isActive: boolean;
};

export type MiseToolSuggestionDto = {
  name: string;
  version: string;
  reason: string;
};

